// ============================================================
// Mobile — Theme Setup
// ============================================================

import { createThemeStore } from '@repo/core/theme';

export const themeStore = createThemeStore('system');
